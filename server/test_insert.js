require('dotenv').config(); const { createPendingUser } = require('./models/Auth/pending_user.model.js'); createPendingUser({ name: 'test', email: 'test_insert@example.com', country: 'PK', mobile_no: '1234567890', password: 'hash', verification_code: 'code', verification_code_expires: new Date(), role_id: 5 }).then(r => { console.log('Inserted:', r); process.exit(0); }).catch(e => { console.error('Insert error:', e.message); process.exit(1); });
